# Project instructions

This project uses React + Vite, Cloudflare Workers, and Cloudflare D1.

Keep Wrangler configuration in `wrangler.jsonc`, use prepared D1 statements, and keep secrets in Cloudflare Worker secrets rather than source files.
